<?xml version="1.0" encoding="utf-8"?>
<!-- Полноэкранный переход при отзыве/возврате прав создателя. Мокап одобрен. Показывается
     ВМЕСТО резкого recreate(): описание + кто вправе + полоса (дольше реального), затем чат
     тихо перезагружается под экраном. Иконка/заголовок/описание — из кода (revoke/restore). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="30dp">

        <FrameLayout
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:background="@drawable/bg_transfer_icon"
            android:layout_marginBottom="22dp">

            <ImageView
                android:id="@+id/iv_icon"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_shield_x"
                app:tint="@color/error"
                android:contentDescription="@null" />
        </FrameLayout>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/revoke_transition_revoke_title"
            android:textColor="@color/text_primary"
            android:textSize="21sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="12dp" />

        <TextView
            android:id="@+id/tv_desc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/revoke_transition_revoke_desc"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:lineSpacingExtra="3dp"
            android:layout_marginBottom="14dp" />

        <TextView
            android:id="@+id/tv_who"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/revoke_transition_who"
            android:textColor="@color/text_tertiary"
            android:textSize="12sp"
            android:lineSpacingExtra="2dp"
            android:layout_marginBottom="30dp" />

        <ProgressBar
            android:id="@+id/pb_revoke"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:progressDrawable="@drawable/bg_progress_bar"
            android:max="100"
            android:progress="0" />
    </LinearLayout>

</FrameLayout>
