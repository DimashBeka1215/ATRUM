<?xml version="1.0" encoding="utf-8"?>
<!-- Плашка «Запрещаю за собой подсматривать» — вместо раздела «Все сообщения» у
     верифицированного разработчика (PERSONAL_BUILD.md §Часть 3). Только токены @color,
     без hardcode — корректна в тёмной и светлой теме. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_settings_card"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="20dp"
    android:layout_marginBottom="16dp"
    android:paddingHorizontal="20dp"
    android:paddingVertical="26dp">

    <ImageView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_shield_x"
        android:contentDescription="@string/stats_dev_no_peek_title"
        app:tint="@color/accent" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:text="@string/stats_dev_no_peek_title"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center"
        android:text="@string/stats_dev_no_peek_subtitle"
        android:textColor="@color/text_tertiary"
        android:textSize="12sp"
        android:lineSpacingMultiplier="1.3" />

</LinearLayout>
