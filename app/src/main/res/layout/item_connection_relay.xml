<?xml version="1.0" encoding="utf-8"?>
<!--
    Одна строка статуса реле на экране «Соединение». Реле обозначено ТОЛЬКО порядковым
    номером ("Реле N") — сам адрес/хост реле сюда никогда не попадает (см. ConnectionStats.kt
    и явное требование пользователя не светить рабочие реле в интерфейсе).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp">

    <ImageView
        android:id="@+id/dotStatus"
        android:layout_width="9dp"
        android:layout_height="9dp"
        android:layout_marginEnd="12dp"
        android:src="@drawable/bg_dot" />

    <TextView
        android:id="@+id/tvRelayLabel"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textSize="14sp" />

    <LinearLayout
        android:id="@+id/sparklineContainer"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:layout_weight="1"
        android:layout_marginHorizontal="10dp"
        android:orientation="horizontal"
        android:gravity="bottom" />

    <TextView
        android:id="@+id/tvRelayLatency"
        android:layout_width="52dp"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textColor="@color/text_secondary"
        android:textSize="12sp" />

</LinearLayout>
